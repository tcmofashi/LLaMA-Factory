# QA生成质量改进文档

## 改进概述

引入 **DS V3.2 模型** 对生成的问题进行智能评分和筛选，显著提升问题质量和深度。

---

## 主要改进点

### 1. 实现真正的 DS V3.2 评分功能

**文件**: `react_agent/qa_pipeline_v2.py`

**函数**: `call_ds_judge(prompt: str) -> Dict`

**功能**:
- 从 `config.toml` 加载 DS V3.2 配置
- 调用 DeepSeek-V3.2 API 进行问题评分
- 解析 JSON 格式的评分结果
- 处理异常情况（返回默认评分，避免流程中断）

**代码示例**:
```python
def call_ds_judge(prompt: str) -> Dict:
    """调用DS V3.2进行问题评分"""
    try:
        import toml
        from pathlib import Path
        from openai import OpenAI

        config_path = Path("/home/tcmofashi/LLaMA-Factory/config.toml")
        with open(config_path, "r", encoding="utf-8") as f:
            config = toml.load(f)

        provider_config = config["providers"]["deepseek"]

        client = OpenAI(
            api_key=provider_config["api_key"],
            base_url=provider_config["base_url"]
        )
        model = provider_config["model"]

        response = client.chat.completions.create(
            model=model,
            messages=[{"role": "user", "content": prompt}],
            temperature=0.3,
            max_tokens=2000
        )

        result = response.choices[0].message.content.strip()

        # 解析JSON结果
        json_match = re.search(r'```json\s*(.*?)\s*```', result, re.DOTALL)
        if json_match:
            result_json = json.loads(json_match.group(1))
        else:
            result_json = json.loads(result)

        return result_json

    except Exception as e:
        print(f"⚠️  DS评分失败: {e}")
        # 返回默认评分（全部通过）
        return {
            "evaluations": [...],
            "approved": [1, 2, 3, 4, 5],
            "total_score": 375,
            "distribution_check": {"passed": True}
        }
```

---

### 2. 修改问题生成 Prompt

**改进内容**:

1. **允许包含背景信息**：
   - 问题可以包含必要的背景信息，使问题更完整、有深度
   - 例如："在京阿尼众多动画作品中，《莉兹与青鸟》以其独特的视觉语言和音乐叙事著称。请分析..."

2. **严禁包含答案**：
   - 明确禁止在问题中直接或间接包含答案
   - ❌ 坏例子："山田尚子执导的《莉兹与青鸟》讲述了什么故事？"（包含答案）
   - ✅ 好例子："请分析《莉兹与青鸟》如何通过音乐与影像的结合，展现两位主人公的内心世界和关系变化。"

3. **添加问题示例对比**：
   - 提供具体的好/坏问题对比
   - 帮助 GLM Agent 理解什么是高质量的问题

---

### 3. 详细的问题审批标准

**DS V3.2 评分标准**:

#### **分布要求**（必须满足）:
- 问题1-3: 必须是 `factual` 类型（整合多个信息点的事实性问题）
- 问题4: 必须是 `summary` 类型（内容概括性问题）
- 问题5: 必须是 `analysis` 类型（主题和艺术分析）

#### **问题深度要求**:
- **90-100分**: 问题有深度、引发思考、包含适当背景信息但不包含答案
- **70-89分**: 问题基本合格，但深度稍欠或背景信息不足
- **60-69分**: 问题过于直白或干巴，但尚可回答
- **0-59分**: 问题质量不合格（反问句、陈述句、包含答案、无法回答等）

#### **问题完整性**:
- 问题必须是完整的疑问句，不能是反问句或陈述句
- 问题必须可以被回答，不能过于模糊或宽泛

#### **背景信息 vs 答案**:
- ✅ 允许：在问题中包含必要的背景信息，使问题更完整、有深度
- ❌ 严禁：在问题中直接或间接包含答案

---

### 4. 智能重新生成机制

**新增函数**: `regenerate_single_question()`

**功能**:
- 根据 DS V3.2 给出的具体改进指令重新生成单个问题
- 保留问题类型（factual/summary/analysis）
- 确保重新生成的问题符合要求

**工作流程**:
1. DS V3.2 评分并标记不合格问题（score < 60）
2. 提供具体的改进指令（regenerate_instructions）
3. 调用 GLM Agent 根据改进指令重新生成
4. 重新评估生成的问题
5. 最多重新生成 10 次，避免无限循环

**改进指令示例**:
```json
{
  "question": 2,
  "score": 45,
  "reason": "问题过于直白干巴，缺少背景信息，深度不足",
  "should_regenerate": true,
  "regenerate_instructions": "请重新生成问题，使其更有深度。可以在问题中加入制作背景、角色设定等信息，使问题更完整。注意不要在问题中包含答案。"
}
```

---

### 5. 完整的审批流程

**文件**: `react_agent/qa_pipeline_v2.py`

**函数**: `generate_questions_for_anime_v2()`

**流程图**:

```
┌─────────────────────────────────────┐
│  步骤1: GLM4.7 生成 5 个问题        │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  步骤2: DS V3.2 质量审批           │
│  - 检查问题类型分布                │
│  - 对每个问题评分（0-100）         │
│  - 标记需要重新生成的问题          │
└──────────────┬──────────────────────┘
               │
               ▼
       ┌───────────────┐
       │ 分布是否正确？ │
       └───────┬───────┘
               │
      ┌────────┴────────┐
      │ No              │ Yes
      ▼                 ▼
┌──────────┐    ┌────────────────────┐
│ 重新生成  │    │ 有不合格问题？     │
│ 整组问题  │    └─────────┬──────────┘
└──────────┘              │
                   ┌──────┴──────┐
                   │ Yes         │ No
                   ▼             ▼
          ┌──────────────┐  ┌──────────┐
          │ 步骤3:       │  │ 步骤5:   │
          │ 智能重新生成 │  │ 保存问题 │
          │ 不合格问题   │  └──────────┘
          └──────┬───────┘
                 │
                 ▼
          ┌──────────────┐
          │ 步骤4:       │
          │ 重新评估     │
          └──────┬───────┘
                 │
                 └──────► [回到步骤5]
```

**关键特性**:

1. **智能评分**: DS V3.2 对每个问题进行详细评分和评价
2. **分布检查**: 确保问题类型分布符合要求（3个factual、1个summary、1个analysis）
3. **精确改进**: 对不合格问题提供具体的改进指令
4. **智能重新生成**: 只重新生成不合格的问题，保留合格问题
5. **重新评估**: 重新生成后再次评分，确保质量
6. **循环控制**: 最多重新生成 10 次，避免无限循环
7. **多轮支持**: 如果整组问题都不合格，最多进行 3 轮重新生成

---

## 使用方法

### 运行完整流程

```bash
cd /home/tcmofashi/LLaMA-Factory

# 运行全流程脚本（包含问题生成和训练数据生成）
python3 react_agent/batch_process_all_anime.py
```

### 只生成问题

```python
import sys
sys.path.insert(0, 'react_agent')
from qa_pipeline_v2 import generate_questions_for_anime_v2

result = generate_questions_for_anime_v2(
    anime_name='孤独摇滚！',
    output_dir='agent_data',
    max_rounds=3
)

print(f"生成了 {result['total_questions']} 个问题")
print(f"总重新生成次数: {result.get('total_regenerations', 0)}")
```

---

## 输出示例

### 评分输出

```
📋 评分结果：

  问题1: 95分 - ✅ 通过
    原因: 问题深度充分，包含适当背景信息但不包含答案，引发思考

  问题2: 45分 - ❌ 需重新生成
    原因: 问题过于直白干巴，缺少背景信息，深度不足
    改进指令: 请重新生成问题，使其更有深度。可以在问题中加入制作背景、角色设定等信息，使问题更完整。注意不要在问题中包含答案。

  问题3: 88分 - ✅ 通过
    原因: 问题基本合格，信息准确，但可以更有深度

  问题4: 92分 - ✅ 通过
    原因: 概括性问题设计合理，覆盖面广

  问题5: 78分 - ✅ 通过
    原因: 分析问题有一定深度，但可以更深入探讨艺术表现手法
```

### 重新生成输出

```
🔄 步骤3: 智能重新生成不合格问题

需要重新生成的问题: [2]

🤖 重新生成问题2...
   改进指令: 请重新生成问题，使其更有深度。可以在问题中加入制作背景、角色设定等信息，使问题更完整。注意不要在问题中包含答案。

✅ 问题2重新生成完成

📊 步骤4: 重新评估更新后的问题

🤖 DS V3.2 正在重新评估...

📋 重新评分结果：

  问题1: 95分 - ✅ 通过
  问题2: 85分 - ✅ 通过
  问题3: 88分 - ✅ 通过
  问题4: 92分 - ✅ 通过
  问题5: 78分 - ✅ 通过
```

### 最终输出

```
====================================================================================================
✅ 第1轮完成！
====================================================================================================
生成问题数: 5
总重新生成次数: 1

✅ 问题已保存到: /home/tcmofashi/LLaMA-Factory/agent_data/孤独摇滚！_questions.jsonl
```

---

## 配置要求

### config.toml

确保配置了 DS V3.2 provider：

```toml
[providers.deepseek]
name = "siliconflow"
model = "Pro/deepseek-ai/DeepSeek-V3.2"
api_key = "sk-..."
base_url = "https://api.siliconflow.cn/v1"
rpm = 60
timeout = 300
```

---

## 评分标准详解

### 1. 分布要求（必须满足）

DS V3.2 会检查问题类型分布：

- **问题1-3**: `factual` - 整合多个信息点的事实性问题
- **问题4**: `summary` - 内容概括性问题
- **问题5**: `analysis` - 主题和艺术分析

如果分布不符合要求，`distribution_check.passed` 为 `false`，整组问题需要重新生成。

### 2. 深度评分

| 分数段 | 描述 | 示例 |
|--------|------|------|
| 90-100分 | 优秀，完全符合所有要求 | "在京阿尼众多动画作品中，《莉兹与青鸟》以其独特的视觉语言和音乐叙事著称。请结合作品的制作背景、导演风格，以及其对原著《吹响吧！上低音号》系列的延续与突破，分析这部剧场版动画在艺术表现和主题表达上的独特之处。" |
| 70-89分 | 良好，基本符合要求，但有小瑕疵 | "《莉兹与青鸟》在音乐表现形式和镜头语言运用上有何独特性？"（缺少背景信息） |
| 60-69分 | 及格，问题过于直白或干巴，但可回答 | "《莉兹与青鸟》的导演是谁？讲述了什么故事？"（过于简单） |
| 0-59分 | 不及格，必须重新生成 | "《莉兹与青鸟》是不是一部关于音乐和友情的动画？"（反问句） |

### 3. 完整性检查

- ✅ **完整的问题**: "请分析《莉兹与青鸟》中音乐与影像的结合如何展现角色关系。"
- ❌ **反问句**: "《莉兹与青鸟》是不是一部优秀的动画作品？"
- ❌ **陈述句**: "《莉兹与青鸟》是一部由山田尚子执导的剧场版动画。"
- ❌ **过于模糊**: "请分析这部动画。"

### 4. 背景信息 vs 答案

| 类型 | 示例 | 评分 |
|------|------|------|
| ✅ 包含背景信息（无答案） | "作为京阿尼旗下备受瞩目的剧场版作品，《莉兹与青鸟》在《吹响吧！上低音号》系列中占据独特位置。请分析...", 85分 |
| ❌ 包含答案 | "山田尚子执导的《莉兹与青鸟》讲述了什么故事？"，0分 |
| ❌ 包含暗示性答案 | "以音乐和友情为主题的《莉兹与青鸟》，其主要内容是什么？"，0分 |

---

## 故障排查

### 问题1: DS评分失败

**现象**: 输出 `⚠️  DS评分失败: ...`

**原因**:
- DeepSeek API 不可用
- API 密钥无效
- 网络连接问题

**解决方案**:
- 检查 `config.toml` 中的 DeepSeek 配置
- 验证 API 密钥是否有效
- 检查网络连接

**Fallback**:
- 如果 DS 评分失败，系统会使用默认评分（全部通过），避免流程中断

### 问题2: 重新生成次数过多

**现象**: 输出 `⚠️  已达到最大重新生成次数，停止重新生成`

**原因**: GLM Agent 生成的质量问题反复不合格

**解决方案**:
- 检查 DS 给出的改进指令是否合理
- 调整问题生成 prompt
- 增加 `max_regenerations` 限制

### 问题3: 类型分布不正确

**现象**: 输出 `⚠️  问题类型分布不符合要求`

**原因**: GLM Agent 生成的问题类型不符合要求

**解决方案**:
- 系统会自动重新生成整组问题
- 检查问题生成 prompt 中的类型说明是否清晰

---

## 性能参考

### 时间消耗

每个动画的处理时间（包含 DS 评分和重新生成）：

- **基础生成**: 2-5 分钟（GLM 生成 5 个问题）
- **DS 评分**: 30-60 秒（评分）
- **重新生成**: 每个不合格问题 1-2 分钟
- **重新评估**: 30-60 秒

**总计**: 约 3-10 分钟（取决于需要重新生成的问题数量）

### 批量处理

- 10 个动画：约 30-100 分钟
- 73 个动画：约 220-730 分钟（3.5-12 小时）

---

## 总结

### 改进效果

1. **问题质量显著提升**: 从简单的直白问题变为有深度、有背景信息的高质量问题
2. **类型分布更准确**: 严格遵守 3个factual、1个summary、1个analysis 的分布要求
3. **智能优化**: 自动识别和改进不合格问题
4. **效率提升**: 只重新生成不合格问题，不浪费合格问题

### 技术亮点

1. **多模型协作**: GLM-4.7 负责生成，DS V3.2 负责评分，形成闭环
2. **智能反馈**: DS 给出具体的改进指令，GLM 根据指令重新生成
3. **循环控制**: 避免无限循环，确保流程稳定
4. **容错机制**: DS 失败时使用默认评分，确保流程不中断

### 下一步优化建议

1. **缓存评分结果**: 相似的问题可以复用评分结果
2. **并行评分**: 多个问题可以并行评分，提升速度
3. **学习机制**: 记录常见的问题和对应的改进指令，形成改进库
4. **更细粒度的评分**: 将问题分为多个维度（深度、完整性、背景信息等）分别评分

---

**最后更新**: 2026-01-01
**文件位置**: `/home/tcmofashi/LLaMA-Factory/QA_QUALITY_IMPROVEMENT.md`
